name: ci

on:
  push:
    branches-ignore:
    - 'release/*'

jobs:
  ci:
    uses: ./.github/workflows/ci-steps.yml
    secrets:
      NGROK_TOKEN: ${{ secrets.NGROK_TOKEN }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
